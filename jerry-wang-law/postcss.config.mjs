// This project styles with plain CSS and needs no PostCSS plugins. The config
// exists so PostCSS stops here instead of walking up and picking up the
// parent repository's Tailwind setup.
export default { plugins: {} };
